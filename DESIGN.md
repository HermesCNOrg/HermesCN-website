# HermesCN 中文社区 — Design Notes

## 目标

当前项目是轻量骨架：前端只保留一个首页，后端只保留用户鉴权相关逻辑。设计文档服务于后续增量开发，避免把旧业务页面、旧视觉系统或不必要的抽象带回来。

## 组件优先级

1. 优先使用 HeroUI 标准组件。
2. 布局和细节样式优先使用 Tailwind CSS utility class。
3. 只有 HeroUI 和 Tailwind 无法清晰表达时，才补充少量全局 CSS 变量或基础样式。
4. 不为单一使用点封装组件、样式文件或中间层。

常用组件选择：

- 操作按钮：`Button`
- 信息容器：`Card`
- 表单输入：HeroUI 表单组件
- 弹层、确认、提示：HeroUI 对应组件
- 链接：语义化 `a` 或 HeroUI `Link`，按页面需要选择

## 视觉方向

- 品牌名称：`HermesCN 中文社区`
- 页面语言：中文优先，英文只在必要技术名词中出现。
- 整体气质：干净、克制、社区感，不做营销落地页式堆叠。
- 当前首页应直接呈现产品/社区状态，不增加无意义 hero 图、长介绍或装饰模块。

## 样式原则

- Tailwind 优先，不新增 CSS module。
- 不为页面单独创建 CSS 文件，除非出现跨页面复用的明确需求。
- 保持现有深色主题基础，避免大面积渐变、装饰光斑和复杂背景。
- 卡片圆角保持克制，默认不超过 8px，除非 HeroUI 组件自带样式。
- 页面内容保持窄而清晰，优先可读性和后续扩展。

## 文案原则

- 用户可见文案保持简洁。
- 避免出现实现术语，除非面向开发者的页面确实需要。
- 翻译内容放在 `src/i18n/messages.json`，翻译逻辑保留在 `src/i18n/messages.ts`。

## 后续新增页面

新增页面前先确认：

- 这个页面是否确实属于当前阶段。
- 是否可以直接使用 HeroUI 组件完成。
- 是否需要国际化文案。
- 是否会引入新的后端数据结构；如果会，同步更新 Prisma schema 和迁移。

## 不做

- 不恢复旧 hackathon/partner/submission 等业务页面。
- 不新增 landing page 式长首页。
- 不引入新的 UI 组件库。
- 不新增一次性 wrapper 或只转发 props 的胶水组件。
- 不把文案硬编码在复杂组件逻辑里。
